.admin-map-container {
  padding: 20px;
  background: #f5f5f5;
  min-height: 100vh;
}

.map-section {
  margin-bottom: 32px;
}

.map-section h3 {
  margin-bottom: 12px;
}

/* NOWY ELEWATOR (N) */
.cells-row.N-top,
.cells-row.N-bottom {
  display: grid;
  grid-template-columns: repeat(10, 70px);
  gap: 8px;
}

/* STARY ELEWATOR (S) — 4 rzędy po 10 */
.cells-row.S {
  display: grid;
  grid-template-columns: repeat(10, 70px);
  gap: 8px;
}

/* KOMORY G — 5 komór */
.cells-row.G {
  display: grid;
  grid-template-columns: repeat(5, 70px);
  gap: 8px;
}

/* KOMORY WYDAWCZE — 10 komór */
.cells-row.W {
  display: grid;
  grid-template-columns: repeat(10, 70px);
  gap: 8px;
}

/* KOMÓRKA */
.cell {
  border: 1px solid #ccc;
  border-radius: 4px;
  padding: 6px 4px;
  background: white;
  text-align: center;
  font-size: 14px;
  cursor: pointer;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: 0.1s ease;
}

.cell:hover {
  transform: scale(1.05);
  box-shadow: 0 0 6px rgba(0,0,0,0.2);
}
@media (max-width: 1024px) {
  .cells-row.S,
  .cells-row.N-top,
  .cells-row.N-bottom,
  .cells-row.W {
    grid-template-columns: repeat(5, 110px);
  }

  .cells-row.G {
    grid-template-columns: repeat(5, 110px);
  }

  .cell {
    width: 110px;
    height: 80px;
    font-size: 16px;
  }
}
@media (max-width: 600px) {
  .cells-row.S,
  .cells-row.N-top,
  .cells-row.N-bottom,
  .cells-row.W,
  .cells-row.G {
    grid-template-columns: repeat(2, 100px);
  }

  .cell {
    width: 100px;
    height: 70px;
    font-size: 14px;
  }

  /* panel boczny jako modal */
  .side-panel {
    width: 100vw !important;
    height: 100vh !important;
    position: fixed !important;
    top: 0;
    right: 0;
    border-radius: 0;
  }
}

/* PANEL OPERATORA — DARK MOBILE UI */

.operator-panel-wrapper {
  padding: 20px;
  background: #111;
  min-height: calc(100vh - 70px);
  color: #eee;
}

.operator-title {
  font-size: 22px;
  font-weight: bold;
  margin-bottom: 20px;
  text-align: center;
  color: #4caf50;
}

/* GRID PRZYCISKÓW */
.operator-buttons {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

/* PRZYCISKI */
.op-btn {
  width: 100%;
  padding: 18px;
  font-size: 18px;
  font-weight: 600;
  border-radius: 10px;
  border: 1px solid #333;
  background: #1a1a1a;
  color: #eee;
  cursor: pointer;
  transition: 0.2s ease;
}

.op-btn:hover {
  background: #222;
  border-color: #4caf50;
  transform: scale(1.02);
}

/* KOLORY FUNKCJI */
.op-intake {
  border-left: 6px solid #4caf50;
}

.op-unload {
  border-left: 6px solid #2196f3;
}

.op-issue {
  border-left: 6px solid #ff9800;
}

.op-correction {
  border-left: 6px solid #e91e63;
}

.op-transfer {
  border-left: 6px solid #9c27b0;
}
